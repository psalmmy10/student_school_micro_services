spring:
  application:
    name: school
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/schooldb
    username: postgres
    password: Adesanya+123.
  jpa:
    hibernate:
      ddl-auto: create
    database: postgresql
  
server:
  port: 8112

eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

application:
  config:
    student-url: http://localhost:8080/api/v1/students

logging:
  level:
    org.springframework.cloud.openfeign: DEBUG


management:
  tracing:
    sampling:
      probability: 1.0   
  zipkin:
    tracing:
      endpoint: http://localhost:9411/api/v2/spans
